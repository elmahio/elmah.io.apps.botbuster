{
	"variables": [],
	"rule": {
		"title": "Automatically ignore errors generated by bots, webcrawlers, spiders and similar.",
		"query": "userAgent: *bot* OR userAgent: *crawl* OR userAgent: *spider* OR userAgent: *search*",
		"then": {
			"type": "ignore"
		}
	}
}